I"�<h1 id="编译">编译</h1>

<h2 id="编译器">编译器</h2>

<p><img src="/img/compiler.png" alt="compiler" /></p>

<p>编译器就是将用高级语言编写的源程序编译成为对应的低级语言（例如机器语言）的程序。</p>

<h4 id="编程语言">编程语言</h4>

<p>编程语言由三方面定义：</p>

<p>1.标记（token）或词位。例如关键字<code class="highlighter-rouge">if</code>，操作符<code class="highlighter-rouge">+</code>，常量如<code class="highlighter-rouge">4</code>和<code class="highlighter-rouge">'c'</code>，还有标识符。</p>

<p>2.语法描述和语言结构。例如类、方法、语句和表达式等。</p>

<p>3.语义描述。描述各种语言结构的意义。</p>
:ET